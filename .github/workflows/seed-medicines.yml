name: Seed Medicine Database on AWS

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "seed" to confirm seeding the medicine database'
        required: true
        default: ''

jobs:
  seed-medicines:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Validate confirmation
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "seed" ]; then
            echo "❌ ERROR: You must type 'seed' to confirm"
            exit 1
          fi
          echo "✅ Confirmation validated"

      - name: Seed Medicine Database on AWS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          command_timeout: 5m
          script: |
            echo "🌱 Starting medicine database seeding on AWS..."

            cd ~/opdwallet_aws || { echo "❌ ERROR: Cannot access opdwallet_aws directory"; exit 1; }

            # Check if medicines.json exists
            if [ ! -f "api/src/scripts/medicines.json" ]; then
              echo "❌ ERROR: medicines.json not found. Please pull latest code first."
              exit 1
            fi

            echo "📊 Clearing existing medicines..."
            docker exec opd-mongodb-prod mongosh opd_wallet --quiet --eval "
              const count = db.medicine_database.countDocuments();
              print('Current medicines count: ' + count);
              db.medicine_database.deleteMany({});
              print('✅ Cleared existing medicines');
            "

            echo "📊 Importing medicines from JSON..."
            # Copy JSON file into the MongoDB container
            docker cp api/src/scripts/medicines.json opd-mongodb-prod:/tmp/medicines.json

            # Import using mongoimport
            docker exec opd-mongodb-prod mongoimport \
              --db opd_wallet \
              --collection medicine_database \
              --file /tmp/medicines.json \
              --jsonArray

            if [ $? -eq 0 ]; then
              echo "✅ Medicine database seeded successfully!"

              # Create text index for search
              echo "📊 Creating text index for search..."
              docker exec opd-mongodb-prod mongosh opd_wallet --quiet --eval "
                db.medicine_database.createIndex({
                  genericName: 'text',
                  brandNames: 'text',
                  searchText: 'text'
                });
                print('✅ Text index created');
              "

              # Verify the count
              echo "🔍 Verifying medicine count in database..."
              docker exec opd-mongodb-prod mongosh opd_wallet --quiet --eval "
                const count = db.medicine_database.countDocuments();
                print('📊 Total medicines in database: ' + count);
              "
            else
              echo "❌ ERROR: Medicine seeding failed"
              exit 1
            fi

            echo "🎉 Medicine database setup complete!"
